root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -tags sqlite_fts5 -o ./tmp/forum ."
  bin = "./tmp/forum"
  include_ext = ["go", "html", "css", "js"]
  exclude_dir = ["tmp", "assets"]
  exclude_regex = ["_test\\.go"]
  delay = 300
  stop_on_error = true

[misc]
  clean_on_exit = true
